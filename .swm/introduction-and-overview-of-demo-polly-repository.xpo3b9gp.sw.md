---
title: DEMO-Polly Repository Overview
---
# DEMO-Polly Repository Overview

## Introduction

This document provides a brief overview of the `DEMO-Polly` repository, owned by the organization `swimmio`. The repository is public and primarily used for the `Polly` project, a .NET resilience and transient-fault-handling library.

## Main Features

`Polly` allows developers to express policies such as Retry, Circuit Breaker, Timeout, Bulkhead Isolation, and Fallback in a fluent and thread-safe manner. The project is predominantly written in `C#`, accounting for 99.8% of the code, with the remaining 0.2% classified as 'Other'.

## Repository Details

The current reference is the `main` branch with the latest commit having the OID `ed1b286bbdf5d70cccc051d81c046f789ffb874e`. For more detailed information about the project and its usage, refer to the repository's `README.md` file.

## Additional Information

The information was gathered on `2024-08-12 11:30:41 UTC` from the URL `https://github.com/swimmio/DEMO-Polly` by the user `edenhermelin`.

<SwmMeta version="3.0.0"><sup>Powered by [Swimm](http://localhost:5000/)</sup></SwmMeta>
